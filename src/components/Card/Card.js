import React from "react";
import "./style.css";

function Card(props) {
    return (
        <div class="card align-left">
            <div class="card-body">
            </div>
        </div>
    );
}

export default Card;
